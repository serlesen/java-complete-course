package com.sergio.javacourse.staticmethods;

public class StaticMethods {
	
	public static void main() {
		
	}
	
	/**
	 * Create a class method to the class Dog with the following specifications:
	 * * it must be named "getColor"
	 * * it must have no input parameters
	 * * it must return a String
	 * * return whatever String you want
	 */
	
	/**
	 * Create a static method to the class Dog with the following specifications:
	 * * it must be named "hasNose"
	 * * it must have no input parameters
	 * * it must return a Boolean
	 * * return whatever Boolean you want
	 */

}
