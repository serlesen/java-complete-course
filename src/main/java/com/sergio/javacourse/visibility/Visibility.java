package com.sergio.javacourse.visibility;

public class Visibility {
	
	public static void main() {
		
	}
	
	/**
	 * Add a method to the class Dog with a visibility of "public" with the following specifications:
	 * * it must be named "getSize"
	 * * it must return an "Integer"
	 * * it must have no input parameter
	 * * return the value you want
	 */
	
	/**
	 * Add a method to the class Dog with a visibility of "private" with the following specifications:
	 * * it must be named "walk"
	 * * it must return "void"
	 * * it must have no input parameter
	 */
	
	/**
	 * Add a method to the class Animal with a visibility of "protected" with the following specifications:
	 * * it must be named "breath"
	 * * it must return "void"
	 * * it must have no input parameter 
	 */

}
